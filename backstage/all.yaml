apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: backstage-all
  description: A collection of all Shieldpay entities, except users, groups, and templates
spec:
  targets:
    # - ./all-apis.yaml # Can be removed in this case
    - ./all-components.yaml
    - ./domains/all-domains.yaml
    # - ./resources/all-resources.yaml
    - ./systems/all-systems.yaml
    - ./templates/all-templates.yaml
